# DeskCat 桌面宠物功能总结报告

## 一、项目概述

DeskCat 是一款 Android 桌面宠物应用，主要面向希望在手机桌面或应用内获得轻量陪伴体验的用户。项目围绕“宠物养成 + 桌面悬浮 + 天气提醒 + 个性化形象”展开，通过宠物状态变化、互动动作、小游戏奖励和天气气泡提示，让宠物具备持续陪伴感和简单养成反馈。

应用采用 Kotlin 和 Jetpack Compose 构建主界面，使用 Android 原生悬浮窗能力实现桌面宠物常驻展示，并结合 DataStore 保存宠物进度和用户设置。

## 二、核心功能模块

### 1. 宠物展示与基础互动

宠物页是应用的核心页面，展示宠物形象、心情状态、对话气泡、金币数量以及三项基础属性：

- 饱腹度
- 开心值
- 精力值

用户可以对宠物进行以下操作：

- 摸摸：提升开心值，轻微消耗饱腹度和精力。
- 喂食：打开食物菜单，选择不同食物恢复属性。
- 玩耍：打开小游戏菜单。
- 休息：恢复精力并改变宠物心情。
- 拖动：在舞台区域内移动宠物位置。
- 重置位置：将宠物恢复到舞台中央附近。

宠物会根据当前状态切换心情，包括放松、开心、兴奋、困倦和饥饿等状态。

### 2. 宠物状态养成

项目通过 `PetStateRepository` 统一管理宠物状态。宠物状态既会受到用户操作影响，也会随时间自然变化。

状态机制包括：

- 用户互动会即时修改饱腹度、开心值、精力值和心情。
- 喂食和小游戏会影响金币数量。
- 离线或空闲一段时间后，宠物状态会自然衰减。
- 宠物会根据当前属性自动选择合适的心情和闲聊内容。
- 互动次数、金币和属性会被持久化保存。

这种设计让宠物不是静态图片，而是具有简单生命周期和养成反馈的桌面角色。

### 3. 食物系统

应用内置食物目录，包含普通猫粮、小鱼干、牛奶、罐头、甜点等食物。每种食物都有独立的：

- 名称
- 描述
- 价格
- 饱腹度增益
- 开心值增益
- 精力值变化
- 图标资源
- 喂食反馈文案

金币不足时无法购买付费食物，宠物会给出对应提示。免费食物可以作为基础恢复手段，付费食物则提供更明显的属性收益。

### 4. 小游戏系统

项目目前包含两个可玩的小游戏：

- 接金币：玩家控制宠物接住金币，根据接到的金币数量获得奖励。若宠物精力过低，奖励会减少。
- 食物老虎机：玩家通过老虎机抽取食物，成功匹配后可获得免费食物奖励。

小游戏与养成系统相互连接：游戏可以获得金币或食物，金币又可以用于购买食物，食物进一步影响宠物状态。

### 5. 天气提醒功能

天气页支持两种天气获取方式：

- 手动输入城市并查询天气。
- 使用设备定位获取当前位置天气。

天气模块会展示城市、天气现象、温度、体感温度、湿度、风向、风力和更新时间。宠物会根据天气生成提醒文案，例如雨天提醒带伞、高温提醒补水、雾霾提醒戴口罩等。

天气还会影响宠物心情，例如舒适晴天更容易让宠物开心，雨雪或雷雨天气会让宠物偏困倦。

### 6. 桌面悬浮窗

悬浮窗是项目区别于普通宠物应用的重要功能。用户授予系统悬浮窗权限后，可以启动 `DesktopPetOverlayService`，让宠物以悬浮窗形式显示在其他应用之上。

悬浮窗支持：

- 前台服务保活。
- 拖动宠物改变位置。
- 点击展开或收起快捷操作区。
- 贴边自动收起。
- 收起后以边缘形态显示。
- 展开后显示宠物气泡和快捷按钮。
- 快捷按钮支持喂食、玩耍、查询天气和打开主应用。
- 根据宠物心情播放不同图片序列或爪子动作。
- 支持自定义图片、资源包和 AI 动画帧同步到悬浮窗。

悬浮窗功能让宠物可以脱离主界面，形成更接近“桌面陪伴”的使用体验。

### 7. 自定义宠物形象

设置页支持从本地选择图片作为宠物形象。应用会尝试对图片进行解码、背景处理和类型分析，并根据识别结果设置宠物风格。

相关能力包括：

- 选择本地图片。
- 持久化图片 URI 权限。
- 解码图片并进行背景处理。
- 使用 ML Kit 进行图像标签识别。
- 根据识别结果区分猫、狗、兔子或默认风格。
- 根据宠物风格调整动画参数。

该功能提高了用户个性化空间，让用户可以把自己的宠物或喜欢的图片设置为桌宠形象。

### 8. 宠物资源包导入

项目支持导入 zip 格式宠物资源包。资源包至少包含：

- `body.png`
- `paw_up.png`
- `paw_down.png`

也可以包含 `pet.json` 来配置爪子位置。资源包导入后会保存到应用内部存储，并由 `PetPackLoader` 加载为可展示的 `PetPack`。

资源包机制相比单张图片更适合制作可动宠物，因为身体和爪子可以分离控制，从而实现挥爪、拍动等动作。

### 9. AI 动画生成

当用户使用自定义图片时，可以调用 AI 动画功能。应用会将宠物图片转换为 Base64，发送给豆包多模态接口，请求生成循环动画规格，再将规格渲染为一组 Bitmap 帧。

AI 动画流程包括：

- 读取当前宠物图片。
- 调用豆包接口生成动画帧描述。
- 解析 JSON 动画规格。
- 将规格渲染为固定尺寸 Bitmap 帧。
- 在主界面和悬浮窗中循环播放。

该功能用于降低用户制作动画资源的门槛，让普通图片也能获得简单动态效果。

### 10. 数据持久化

项目使用 DataStore Preferences 保存数据，主要包括：

- 宠物饱腹度
- 开心值
- 精力值
- 金币数量
- 互动次数
- 上次更新时间
- 自定义图片 URI
- 宠物大小
- 自动移动开关
- 宠物资源包路径
- 天气城市配置

持久化机制保证用户关闭应用后再次进入时，宠物状态、金币和设置仍然保留。

## 三、界面结构

应用主界面使用底部导航栏划分为三个页面：

- 宠物页：展示宠物状态、互动按钮和宠物舞台。
- 天气页：查询天气并让宠物给出天气提醒。
- 设置页：管理悬浮窗、自定义形象、资源包、AI 动画、宠物大小和自动移动。

当进入小游戏时，界面会切换到独立游戏模式，游戏结束后返回宠物页并结算奖励。

## 四、技术实现特点

### 1. 状态集中管理

宠物状态集中在 `PetStateRepository` 中维护，界面层通过 `StateFlow` 订阅状态变化。这种方式便于主界面、悬浮窗和其他模块共享同一份宠物状态。

### 2. Compose 与原生 View 结合

主界面使用 Jetpack Compose 实现，悬浮窗使用 Android 原生 View 和 `WindowManager` 实现。两者通过状态仓库和设置仓库进行数据同步。

### 3. 模块职责清晰

项目按功能拆分为宠物状态、设置、悬浮窗、天气、小游戏、资源包和 AI 动画等模块。每个模块独立处理自己的数据和业务逻辑，便于后续扩展。

### 4. 外部能力接入

项目接入了天气接口、设备定位、ML Kit 图片识别和豆包多模态接口，使桌宠具备环境感知和个性化生成能力。

## 五、当前功能亮点

- 桌面悬浮窗让宠物可以常驻在其他应用之上，符合桌面宠物定位。
- 宠物状态会随时间自然衰减，具有基础养成感。
- 天气查询不只是展示数据，还会转化为宠物语气的提醒。
- 自定义图片和资源包导入提升了个性化程度。
- 小游戏、金币和食物形成了基本的玩法闭环。
- AI 动画生成拓展了自定义形象的表现力。

## 六、可扩展方向

后续可以优先扩展以下功能：

- 等级与经验系统：根据互动、喂食、小游戏和天气查询获得经验，解锁新动作或外观。
- 每日任务系统：设置每日摸摸、喂食、小游戏、天气查询等任务，完成后奖励金币。
- 亲密度系统：记录长期陪伴关系，让宠物台词、动作和反馈逐渐变化。
- 成就系统：围绕连续登录、金币收集、资源包导入、天气查询等行为设置成就。
- 皮肤和图鉴：支持多个宠物资源包管理，并提供皮肤预览、切换和删除。
- 悬浮窗增强：增加双击摸摸、长按菜单、自动休息、边缘提醒等交互。
- 更多小游戏：加入逗猫棒、记忆翻牌、反应点击等轻量玩法。
- 天气深度联动：让天气不仅影响台词，也影响宠物属性变化和动作表现。

## 七、总结

DeskCat 已经实现了桌面宠物应用的核心能力，包括宠物展示、状态养成、互动动作、桌面悬浮、天气提醒、小游戏、自定义形象和 AI 动画。项目功能之间已经形成基础闭环：用户通过互动和小游戏获得金币，使用金币购买食物照顾宠物，宠物状态影响心情和反馈，悬浮窗则把这种陪伴体验延伸到主应用之外。

从当前完成度来看，项目已经具备较完整的课程展示和代码上传价值。后续如果继续开发，可以重点围绕成长系统、任务系统和悬浮窗交互增强进行迭代，让桌宠从“可互动”进一步发展为“可长期陪伴”。

