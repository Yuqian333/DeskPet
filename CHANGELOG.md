# 更新日志

## 2026-06-11

### 新增

- 新增 AI 透明桌宠帧生成链路：上传宠物图片后压缩并转 Base64，结合默认或自定义提示词调用豆包图片生成接口。
- 新增 AI 动画历史库：每次生成的动画保存为独立版本，设置页可长期查看和管理。
- 新增动画版本管理能力：支持切换、重命名、删除历史动画，并使用第一张生成帧作为版本封面。
- 新增宠物命名设置：默认名为“小猫”，主界面、天气提醒、小游戏反馈和 AI 提示词会使用用户设置的名字。
- 新增 `local.properties.example`，说明天气、remove.bg 和豆包图片生成接口配置项。

### 优化

- AI 动画默认生成简洁黑白线稿风格，动作幅度较小，播放顺序为 `1 -> 2 -> 3 -> 2 -> 1`。
- 生成帧保存前会进行透明背景后处理：优先保留有效 alpha，其次尝试 remove.bg，最后使用本地白底/浅色背景去除兜底。
- 透明检测改为识别边缘背景透明比例，避免少量透明像素导致白底图被误判为透明图。
- 扩展宠物气泡、天气提醒和小游戏文案，整体语气更可爱、活泼和陪伴化。
- 设置页的 AI 动画库改为常驻显示，不再依赖是否上传过图片。
- 优化金币显示宽度，避免个位数被挤压或裁切。

### 修复

- 修复生成新 AI 动画后旧动画无法再次选择的问题。
- 修复悬浮窗在设置刷新时可能覆盖当前 AI 动画帧的问题。
- 修复未配置 remove.bg 时白底 AI 生成帧无法透明化的问题。
- 修复无宠物图片时点击生成 AI 动画没有明确反馈的问题。

### 验证

- 已运行 `assembleDebug`，确认 Android Debug 包构建通过。
